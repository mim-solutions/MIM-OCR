name: Build Base Image
run-name: Build base image
on: [workflow_dispatch]
jobs:
  Build-Base-Image:
    runs-on: ubuntu-20.04
    steps:
      - run: echo "Starting docker build."
      - run: docker build --tag $CI_REGISTRY_IMAGE:base_ubuntu -f build/base_image/base_ubuntu.Dockerfile .