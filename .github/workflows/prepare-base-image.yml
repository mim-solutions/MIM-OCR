name: Build Base Image
run-name: Build base image
on: [workflow_dispatch]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}_base_image

jobs:
  Build-Base-Image:
    runs-on: ubuntu-20.04
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."

      - name: Log in to the Container registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }

      -
        name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: .
          file: build/base_image/base_image_python.Dockerfile
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

#      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
#      - run: echo "Starting docker build."
#      - run: docker build --tag aaa:base_ubuntu -f ${{ github.workspace }}/build/base_image/base_ubuntu.Dockerfile .
#